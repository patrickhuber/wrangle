package global

const (
	// PackagePathKey defines the environment variable for the package path
	PackagePathKey = "WRANGLE_PACKAGE_PATH"
	// ConfigFileKey defines the environment variable for the current configuration
	ConfigFileKey = "WRANGLE_CONFIG"
)
